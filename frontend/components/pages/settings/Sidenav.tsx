import NavList from "./NavList";

// Sidenav with all the links

const Sidenav = () => {
  return (
    <aside className="settings-sidenav">
      <NavList />
    </aside>
  );
};

export default Sidenav;

import React from "react";

// Check if logged in user matches [profile] username, then return different components such as edit

const Profile = () => {
  return <div className="profile-page">moi</div>;
};

export default Profile;

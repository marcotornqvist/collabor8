export enum ErrorStatus {
  success = "success",
  warning = "warning",
  danger = "danger",
  default = "default",
}

export enum ProfileStatus {
  Auth = "Auth",
  User = "User",
  Guest = "Guest",
}